---
name: solar-power-evaluation-claude
description: 光伏项目投资评估。适用场景：用户要求评估光伏项目或上传项目基本信息时；需综合搜索电力市场数据（限电率、电价、竞价成交价）编制财务假设；执行2任务财务测算（投资边界/出售边界）；输出投资决策阈值及评估报告。集中式、分布式、渔光互补、农光互补等各类光伏项目均适用。
---

> **硬性要求①**：执行本技能前，确认当前会话使用 `deepseek-v4-pro` 模型 + 思考强度 `max`（用户可用 `/model deepseek-v4-pro` 和 `/reasoning xhigh` 切换）。评估全程不得使用其他模型。如发现模型不对，先提示用户切换再继续。
>
> **硬性要求②（🔴 铁律）**：**禁止使用浏览器搜索**。所有市场数据（电价、限电率、利用小时数、规划文件等）的搜索**只能通过 Tavily API**（`curl -s https://api.tavily.com/search`）。不得使用 `browser` 工具访问网站。Tavily API Key：`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`。每类搜索不少于10个关键词，每次调用 `search_depth=advanced, max_results=8~10`。

# 光伏项目投资评估

按照以下步骤完成光伏项目投资评估。

## 第零步：前置准备（每次评估前必须执行）

### 0.1 搜索 Obsidian 知识库

在开始任何搜索之前，先搜索 Obsidian 知识库中是否有该项目的已有资料：

- 在 `entities/` 目录下搜索项目名、省名、投资方
- 在 `concepts/` 目录下搜索该省已有的新能源规划、电网规划文件、机制电价竞价文件
- 在 `topics/` 目录下搜索该省已有的电力市场分析、机制电价竞价文件

如果有已有的项目页面或参数文件，优先提取项目基本信息，和用户确认后再进行下一步

**知识库读取限制（硬性规定）**：
- 从 Obsidian 知识库**只能提取**项目所在地和装机容量，用于引导后续搜索
- 以下三类信息**禁止**从知识库提取，必须通过独立搜索获取：
  - 等效利用小时数
  - 电价
  - 投资概算
- 其他信息（接入点、用地面积、组件类型、建设周期、外线距离、地形地貌等）可从知识库提取，作为辅助判断依据

### 0.2 获取省级规划文件及电价信息

对于新省份（Obsidian 中无该省规划或电价文件时），搜索并摄取三份关键官方文件：

1. **省级最新新能源规划**：
   - 搜索 `{省份} 新能源 发展 规划 2025 2026`
   - 搜索 `{省份} 十四五 十五五 新能源 装机目标`
   - 搜索 `{省份} 风电 光伏 发展 规划`

2. **省级最新电网建设规划**：
   - 搜索 `{省份} 电网 建设 规划 外送 特高压 2025 2026`
   - 搜索 `{省份} 电力 外送 通道 消纳`
   - 搜索 `{省份} 配电网 储能 规划`

3. **省级最新机制电价竞价结果**：\n   - 搜索 `{省份} 光伏 机制电价 机制电量 机制电价有效期 机制电价时间 竞价 成交价`\n   - 搜索 `{省份} 光伏项目 光伏现货价格 光伏中长期价格`\n\n提取关键信息（装机目标、外送通道规划、配储要求等），写入 Obsidian：\n- `concepts/{省份}新能源规划.md`\n- `concepts/{省份}电网规划.md`\n- `concepts/{省份}电价信息.md`\n\n如 Obsidian 中已有这三个文件（6个月内的），跳过搜索和摄取。\n\n**参考文件**：`references/广东省机制电价竞价细则.md` 包含广东省2025年首轮竞价的核心参数（电价0.36元/kWh、机制电量比例上限80%、执行期限12年、CfD差价机制等），广东省项目可参考此文件。

## 第一步：获取项目基本信息

询问用户以下必要信息（如未提供）：

1. **项目所在地**：精确到省份、城市/地区（例如：新疆哈密、青海海南州、山东东营等）。这是搜索电力市场数据的关键依据。
2. **项目装机容量**：以MW为单位（例如：50MW、100MW、200MW），说明是直流侧还是交流侧容量。
3. **项目类型**（新增）：集中式地面电站 / 工商业分布式 / 户用分布式 / 渔光互补 / 农光互补 / 沙戈荒大基地。不同类型影响限电率判断和投资区间。
4. **项目接入方案**（可选）：用户可提供新建线路接入变电站的信息，辅助判断断面阻塞情况。

如用户上传包含项目信息的文件（Excel、Word、PDF等），优先从文件中提取。

---

### 🔍 搜索工具回退说明

本技能多处引用 `web_search`。在网关环境（飞书/Telegram）下若 `web_search` 不可用，使用 Tavily API 直接调用回退（代码见注意事项第2条），或使用 `delegate_task` 委托子代理搜索。搜索逻辑和判定标准不变。

### 1.2 等效利用小时数获取（硬性规定）

> ⚠️ **先区分两个概念**：
> - **理论利用小时数**(h) = 项目所在地最佳倾角年斜面辐照量(GTI, kWh/m²) × Performance Ratio(PR, 典型0.80~0.82) / 1kW/m²。这是理想条件下的最大可能值，**不扣除任何衰减、限电或损失**。
> - **等效利用小时数**(h) = 进入财务模型的实际值。已经考虑了限电、遮挡、灰尘、组件失配、线损等综合损失，**小于**理论利用小时数。
> 
> 隐含限电率 = `1 - (等效利用小时数 / 理论利用小时数)`。如果这个值明显高于搜索到的实际限电率，说明等效利用小时数可能偏保守了。

光伏等效利用小时数基于以下方法推算，**必须交叉验证**。分**三步**获取：

**第一步：Solargis/NASA 资源数据基础（优先方法）**
- 项目所在地的等效利用小时数应以 **Solargis 或 NASA POWER 数据库**的最佳倾角斜面辐照量为基础
- 搜索或直接查询：
  - `{项目所在地} 光伏 最佳倾角`
  - 潮州最佳倾角典型值约19°，汕头/揭阳等粤东地区17-19°，广东大部17-20°
  - 查询最佳倾角下的**年斜面总辐照量（GTI）**，单位为 kWh/m²/年
  - 或直接找该地**最佳倾角等效利用小时数速查表**
- **保守规则**：将最佳倾角下的等效利用小时数**减去100小时**，作为输入财务模型的保守值
  - 例：潮州速查表1,153.4h → 减100h → 1,053h（取整1,050h）
  - 原因是Solargis/NASA理论值基于标准条件，实际运行中有遮挡、灰尘、组件失配、线损等不可预见损失
  - 如果减100后的数值低于项目简介宣称值，仍以减100后的值为准
  - 如果减100后的数值高于项目简介宣称值，取两者中较低者

**第二步：web_search 搜索验证（辅助方法）**
- 通过 `web_search` 搜索验证（不少于5个不同类型的来源）：
  - 搜索 `{省份} 光伏 等效利用小时数 2024 2025`
  - 搜索 `{项目所在地} 光伏电站 年发电量 等效利用小时数`
  - 搜索 `全国新能源消纳监测预警中心 {省份} 光伏 利用小时数`
- 与第一步的Solargis基准值进行比对
- 取**两者中的较低值**输入财务模型（保守原则）

**第三步：交叉验证限电率**
- 用理论值和实际值计算隐含限电率：
  ```
  理论利用小时数 = GTI(kWh/m²) × PR(~0.80~0.82) / 1kW/m²
  隐含限电率 = 1 - (等效利用小时数 / 理论利用小时数)
  ```
  其中**等效利用小时数**= 你从第一、二步确定拟输入财务模型的值（已减100h保守调整）
- 与通过限电率估算（见第三步）得到的限电率交叉验证
- 若两者接近 → 数据可信；若差异大 → 需分析原因，取保守值

**最终输入**：等效利用小时数 = min(Solargis减100h值, web搜索值, 项目简介值)

> ⚠️ **常见错误警示**：不要直接把项目简介宣称的利用小时数输入财务模型。项目简介通常偏乐观（如广东项目宣称1,300h，但Solargis速查表仅1,153h），必须取保守值。

### 1.3 组件衰减率

光伏项目特有的参数——组件年衰减率。默认取值 **0.5%/年**（线性衰减），对应主流 N 型 TOPCon 组件首年衰减 1%、此后年均 0.4% 的保守折中。如需调整，参考：
- P 型 PERC：0.55-0.7%/年（首年 2% + 此后 0.45-0.55%）
- N 型 TOPCon：0.4-0.5%/年（首年 1% + 此后 0.4%）
- HJT：0.35-0.45%/年

如用户提供了具体组件型号，以厂商质保数据的上限为准。

### 1.4 电价获取（硬性规定）

电价获取规定：
- **禁止**从 Obsidian 知识库中可研类报告直接提取（可信度不足）
- 可从知识库中其他类型知识（如市场分析文章、行业数据汇编）提取电价参考值
- 必须通过 `web_search` 多源搜索验证：**机制电价、年度中长期价格、现货价三类，每类不少于10个不同类型的来源**
- 知识库参考值与搜索值比对，取保守值（较低者）
- 优先采用官方来源（国家能源局、省级发改委、电力交易中心）
- 存在较大差异时取保守值（较低电价）

## 第二步：搜索电力市场数据

### 2.0 消纳能力分级判断

根据项目所在省情况，采用不同搜索策略：

**消纳友好省（近负荷中心）**：河南、山东、江苏、广东、浙江、福建、湖北、湖南、江西、安徽等 → 光伏限电率通常 5-10%，搜索重点：省内断面、午间负荷调节能力

**消纳承压省（外送依赖型）**：甘肃、新疆、宁夏、内蒙古、青海、陕西、山西、河北等 → 光伏限电率通常 8-25%+（光伏限电普遍高于同省风电 2-5 个百分点），搜索重点：**外送特高压进展、午间倒负荷、储能配置、大基地配套消纳**

**省内区域差异**（重要）：同一省内光伏限电率差异显著，不可用省均值一概而论。例：
- 甘肃河西（酒泉/嘉峪关/张掖）光伏限电率可达 20-30%，河东（陇南/天水）降至 8-12%
- 新疆南疆（喀什/和田）光伏限电高于北疆（阿勒泰/塔城）
- 青海海西限电高于海南

如项目位于消纳承压省，额外专项搜索：
- `{省份} 弃光 原因 分析 2026`
- `{省份} 午间 光伏 限电 时段 2026`
- `{省份} 外送 特高压 建设 进展 2026`
- `{省份} 储能 配置 光伏 消纳`

基于项目所在地，使用 `web_search` 多次搜索并交叉验证：

### 2.1 限电率/消纳情况

搜索关键词示例：
- `{项目所在地} 光伏 限电率 {当前年份}`
- `{项目所在地} 新能源 弃光率`
- `{项目所在地} 光伏消纳能力`
- `{项目所在地} 电网断面阻塞 光伏`
- `{省份} 光伏 午间 调峰 限电`

分析要点：查找近2年弃光率数据、消纳能力报告、断面阻塞情况、全国新能源消纳监测预警中心数据。光伏限电集中在午间 11:00-14:00 时段，严重程度与当地光伏渗透率正相关。

### 2.2 电价信息

电价信息获取规定见 **1.4节**。本节仅作为搜索关键词参考。

搜索三类电价，每类不少于10个来源，交叉验证：

**中长期交易电价**：
- `{项目所在地} 光伏中长期交易均价 {当前年份}`
- `{省份} 年度光伏中长期交易 成交均价`

**现货市场电价**：
- `{项目所在地} 光伏现货交易均价 {当前年份}`
- `{省份} 电力现货市场 光伏 午间 电价`

注意：光伏现货电价受午间出力集中影响，通常低于风电现货价格和全省现货均价。

**绿电交易电价**：
- `{项目所在地} 绿电交易均价 {当前年份}`
- `{省份} 绿色电力交易 成交均价`

### 2.3 机制电价竞价信息

- `{省份} 光伏 机制电价 机制电量 机制电价有效期 竞价 成交价`
- `{省份} 光伏项目 上网电价 竞价结果`

### 2.4 数据交叉验证

- 每类数据不少于10个不同类型来源交叉验证
- 优先采用官方来源（国家能源局、省级发改委、电力交易中心）
- 存在较大差异时取保守值（较低电价、较高限电率、较低利用小时）
- 以表格呈现最终确定的电力市场数据

## 第三步：确定关键参数

### 限电率估算（双轨并行）

**轨道A——项目所在地评估限电率**：
结合接入方案、断面阻塞、消纳能力和省内区域位置：
- **基础四档**（光伏专用，较风电上调一档）：
  - ① 消纳能力≥年发电量1.5倍、无断面阻塞 → 1%-2%
  - ② 消纳能力为1.2-1.5倍、轻微断面阻塞 → 3%-5%
  - ③ 消纳能力为1.0-1.2倍、中等断面阻塞 → 6%-10%
  - ④ 消纳能力<1.0倍、严重断面阻塞 → 15%-25%
- **省内区域调整系数**：
  - 同省内，项目位于**消纳较好区域**（近负荷/近外送汇集站/省内南部/东部）→ 在基础档位下限取值
  - 项目位于**消纳较差区域**（远负荷/近电源集中区/省内西部/北部）→ 在基础档位上限取值
  - 调整幅度一般在基础值的 ±15-30%
- **光伏额外加码因子**：
  - 当地光伏渗透率（光伏装机/最大负荷）> 50% → 在基础档位上加 3-5 个百分点（午间限电恶化）
  - 项目为**全额上网分布式**且接入 10kV 及以下配电网 → 加 2-3 个百分点（配电网反向重载风险）

**轨道B——全省同类型电源平均限电率**：
- 搜索 `{省份} 光伏 全省 平均 限电率 {近两年}`
- 搜索 `全国新能源消纳监测预警中心 {省份} 光伏 利用小时数 限电率`
- 搜索 `{省份} 光伏 弃光率 全省均值`
- 搜索 `{省份} 光伏 限电率 {近两年}年`
- 累计不少于10个不同类型来源交叉验证，取保守值（**较高限电率**）

> ⚠️ **🔴 铁律：不盲信全国消纳监测数据**。该中心公布的"光伏利用率"（如山东98.5%）是**全国平均口径**，对光伏渗透率极高/午间限电严重的省份严重低估真实限电。必须交叉验证以下**地方精细化来源**，取其中**最高限电率**：
> 1. **冬雪说-新能源与电**公众号的「每日一地：{省份}光伏限电率分析」系列（按地市/基地级别分析）
> 2. **风芒能源、储能与电力市场**等垂直媒体的区域限电报道
> 3. **济宁领跑者基地等标杆项目的利用小时同比降幅**（资源因素vs限电因素拆解）
> 4. 各省电力交易中心公布的**新能源结算情况**
> 
> **实例**：2025年山东光伏，全国消纳监测中心称利用率98.5%（限电率1.5%），但济宁领跑者基地利用小时同比降202h（资源因素仅~20h），隐含限电严重得多。最终取值应远高于1.5%。

**最终限电率取值（强制校验）**：
> 将以下三者取**最高值**进入财务模型：
> 1. 轨道A（项目所在地评估）限电率
> 2. 轨道B（全省均值）限电率
> 3. 第1.2步隐含限电率 = `1 - (实际小时数 / 理论小时数)`

- 以**轨道B（全省均值）**为主要参考基准
- 轨道A作为校核：若估算值显著低于轨道B，可适当下调，但幅度不超过30%
- 若轨道A估算值高于轨道B，取轨道A
- 追加与第1.2步隐含限电率的比对，取三者中**最高值**（保守原则）
- 所有取值均注明依据和来源

### 电价策略

**⚠️ 重要：机制电价不是固定保底电价（CfD差价合约机制）**
根据136号文，机制电价本质是差价合约（Contract for Difference），不是固定保底价。实际到手收入 = 项目在节点上的市场化交易收入 + CfD差价补偿。后者是全省月均同类电源现货价与竞价中标价之间的差额。详见 `references/全国各省机制电价参考.md`。

**情况一**（不参与机制电价竞价，纯市场化）：
- 年度中长期交易 70%，按当地光伏中长协均价
- 现货交易 30%，按当地光伏现货均价（注意午间时段价格偏低）
- 绿电交易 100%覆盖（与中长协/现货叠加），按绿电均价
- 扣除两个细则考核（0.015元/kWh）和市场费用分摊（结算电费×2%）

**情况二**（参与机制电价竞价，含CfD差价合约保护）：

分**两段**计算（机制期内 + 机制到期后）：

#### 机制期内（前N年，各省不同）
- **机制电量比例 ≠ 100%**。各省政策对不同类型项目设定了申报比例上限，例如：
  - 广东（2025年竞价）：110kV以下分布式光伏 → **80%**上限；其他分布式 → 70%上限
  - 必须在搜索结果中确认该省的具体规定
  - 搜索关键词 `{省份} 机制电量 比例 上限 分布式 110千伏`
- **机制电价**：按该省竞价结果执行（如广东分布式光伏0.36元/kWh）
- **剩余电量**（如广东20%）：按市场化电价（现货价）结算
- **执行期限**：各省不同。例如广东光伏项目12年，海上风电14年
- 扣除两个细则考核（0.015元/kWh）和市场费用分摊（结算电费×2%）

> ⚠️ **常见错误**：不要默认机制电量比例为100%。应搜索确认各省政策。广东2025年竞价明确110kV以下分布式上限80%。
> 详见 `references/广东省机制电价竞价细则.md`。

#### 机制到期后（剩余运营年限）
机制电价执行期限到期后，项目**全部电量按市场化方式结算**：
- **一定比例参与中长期交易**（典型60-70%，在报告中需说明假设依据）
- **剩余部分参与现货市场**
- 中长期价：参考该省年度交易均价，但注意光伏午间出力集中会导致光伏中长期价低于全省综合均价（需做保守调整）
- 现货价：参考该省光伏现货实时市场价（如广东2026年2月仅0.108元/kWh）

**有效电价计算**（分段平均）：
```
前N年有效电价 = [机制电量比例 × 机制价 + (1-机制比例) × 现货价 - 考核] × (1-市场费率)
后M年有效电价 = [中长期比例 × 中长期价 + (1-中长期比例) × 现货价 - 考核] × (1-市场费率)
运营期加权均值 = (N × 前N年有效电价 + M × 后M年有效电价) / 运营期年限
```

> 🔴 **必须执行：两个扣减不可遗漏**。`有效电价 = (加权均价 - penalty - 考核) × (1 - market_fee_rate)`。其中：
> - **两个细则考核**（penalty）：减法项，默认 **0.015元/kWh**
> - **市场费用分摊**（market_fee_rate）：乘法项，默认 **结算电费×2%**
> 
> **常见陷阱**：
> 1. 只算了加权均价，忘了减 penalty 和乘 (1-market_fee_rate) → 有效电价偏高
> 2. 把 penalty 和 market_fee_rate 混在加权均价里处理 → 概念错误
> 3. 检查 `financial_model.py` 的 `effective_price_base` 计算确认是否包含了这两个扣减

> ⚠️ **常见错误**：不要用运营期平均值一刀切处理。前N年机制价和后M年市场化价差异巨大（广东光伏机制价0.36 vs 现货价0.108），必须分段计算或输入分段模型。使用脚本时，如果脚本只支持单价格，需手动计算分段加权均价作为替代，并在报告中说明。

### 财务参数

| 参数 | 数值 | 说明 |
|------|------|------|
| 经营期 | 25年 | 光伏组件技术寿命主流25年，按25年测算经营期 |
| 折旧年限 | 20年 | 残值率5%，直线法（25年经营期，前20年折旧，后5年无折旧） |
| 融资利率 | 4% | 固定利率 |
| 融资期限 | 15年 | 等额本金还款 |
| 增值税率 | 13% | — |
| 附加税率 | 12% | 城建+教育+地方教育（增值税应纳税额） |
| 所得税率 | 25% | 不享受三免三减半和即征即退 |
| WACC | 5.5% | 全投资 |
| 资本金成本 | 7% | 权益端 |

### 运维费与保险费

- 运维费：1-5年 0.02元/W·年，6-10年 0.025元/W·年，11-25年 0.03元/W·年
- 逆变器替换：第12年一次性全部更换（费用=装机容量×0.05元/W），整个生命周期仅此一次。逆变器设计寿命约12-15年，25年经营期中间点更换一次。
- 保险费：当年项目净值×0.2%（净值=初始投资-累计折旧，折旧按20年直线法，残值5%）

> ⚠️ 25年经营期注意：逆变器设计寿命约12-15年，在第12年一次性全部更换，整个生命周期仅此一次。

### 融资假设

- **假设1**：融资杠杆率100%（全额融资），融资15年，利率4%，等额本金
- **假设2**：融资杠杆率80%（融资80%，资本金20%），融资15年，利率4%，等额本金

### 增值税抵扣规则

光伏项目同样采用融资租赁模式，进项税抵扣规则与风电一致：

每年增值税销项税可抵扣金额为当年付息金额的13% + 运维费的6%（运维服务为主适用6%税率），可抵扣金额可结转下一年使用，直至融资偿还完毕。

逆变器替换费用（第12年）所含增值税亦可作为进项抵扣。

### 收益率参数

- 全投资WACC = 5.5%
- 资本金成本 = 7%

## 第四步：执行核心测算

使用 `scripts/financial_model.py` 执行财务模型，对2个任务分别进行迭代计算，精度到小数点后4位。

---

### 任务1（投资边界）：100%融资，最小DSCR≥1.2

**约束条件**：
- 偿债备付率 = 可用于还款的净现金流量 / 当年应还本付息金额
- 经营期内最小DSCR ≥ 1.2

**输出指标**：
- 最高单瓦投资金额、度电成本、全投资IRR、年均净现金流量、年均净利润

**每年净利润表（含公式）**：

| 公式 | 说明 |
|------|------|
| `发电量 = 装机容量×1000×等效利用小时×(1-限电率)×(1-衰减率)^年数` | 逐年衰减 |
| `营业收入 = 年发电量 × 有效电价` | 有效电价已含绿电溢价，扣除罚款和市场费 |
| `运维费 = 装机容量 × 单位运维费率` | 分段：1-5年0.02元/W·年，6-10年0.025元/W·年，11-25年0.03元/W·年 |
| `第12年运维费追加 = 装机容量 × 0.05元/W` | 第12年逆变器一次性全部替换（生命周期仅此一次） |
| `保险费 = 项目净值 × 0.2%` | 净值 = 初始投资 - 累计折旧 |
| `折旧 = 总投资 × (1-残值率) / 20年` | 直线法，残值率5%（20年折完，后5年无折旧） |
| `利息 = 期初剩余本金 × 利率` | 等额本金还款 |
| `增值税销项 = 营业收入 × 13% / 1.13` | |
| `增值税可抵扣 = 当年利息×13% + 运维费×6%` | 可结转下一年，直至融资偿还完毕 |
| `增值税及附加 = max(0, 增值税销项 - 累计可抵扣) × 12%` | |
| `所得税 = max(0, (营业收入-运维费-保险费-折旧-利息-增值税及附加) × 25%` | |
| `净利润 = 营业收入 - 运维费 - 保险费 - 折旧 - 利息 - 增值税及附加 - 所得税` | |

**每年净现金流量表（含公式）**：

| 公式 | 说明 |
|------|------|
| `EBITDA = 营业收入 - 运维费 - 保险费` | |
| `可用于还款 = EBITDA - 增值税及附加 - 所得税` | |
| `当年应还本金 = 期初本金 / 18年` | 等额本金 |
| `当年应还利息 = 期初剩余本金 × 利率` | |
| `当年应还本息 = 当年应还本金 + 当年应还利息` | |
| `偿债备付率 = 可用于还款 / 当年应还本息` | |
| `净现金流量 = EBITDA - 增值税及附加 - 所得税 - 当年利息 - 当年偿还本金` | |

---

### 任务4（出售边界）：80%融资+20%资本金，全投资IRR≥6%且资本金IRR≥8%

**约束条件**：
- 全投资IRR ≥ 6%
- 税后资本金IRR ≥ 8%

**输出指标**：
- 目标单瓦投资金额、度电成本、税后资本金IRR、全投资IRR、年均净现金流量、年均净利润

**每年净利润表**（公式同上）

**每年净现金流量表（含公式）**：

| 公式 | 说明 |
|------|------|
| `EBITDA = 营业收入 - 运维费 - 保险费` | |
| `可用于还款 = EBITDA - 增值税及附加 - 所得税` | |
| `当年应还本金 = 期初本金 / 18年` | |
| `当年应还利息 = 期初剩余本金 × 利率` | |
| `当年应还本息 = 当年应还本金 + 当年应还利息` | |
| `偿债备付率 = 可用于还款 / 当年应还本息` | |
| `股权现金流 = 净利润 + 折旧 - 当年偿还本金` | 净利润已含利息，折旧未付现 |

---

### 迭代搜索

完成市场数据搜索后，搜索当前光伏行业单瓦投资成本作为参考：
- `{当前年份} 光伏项目 单瓦投资成本 元/W`
- `{省份} 光伏 单位千瓦投资`
- `光伏组件 价格 元/W {当前年份}`

根据搜索结果确定初始迭代范围。集中式光伏通常在 2.5-3.5 元/W，分布式在 2.0-3.5 元/W（2026年组件价格回暖至0.83-0.92元/W，带动系统造价上行）。

## 第五步：输出结果

### 说明测算逻辑

说明以下内容：
- 限电率估算依据（引用搜索到的数据来源）
- 电价策略及具体应用的各项电价（附来源），注意光伏现货电价午间偏低的特点
- 当地消纳能力和断面阻塞情况，特别关注光伏渗透率和午间调峰能力
- 财务模型关键假设（含组件衰减率、逆变器替换【第12年一次性全部替换，0.05元/W】）
- 迭代计算方法和收敛过程

### 两个任务结果

以表格形式分别输出2个任务的结果：

**任务1（投资边界）：100%融资，最小DSCR≥1.2**
| 指标 | 数值 |
|------|------|
| 最高单瓦投资金额（元/W） | {X} |
| 度电成本（元/kWh） | {X} |
| 全投资IRR（%） | {X} |
| 年平均净现金流量（万元） | {X} |
| 年平均净利润（万元） | {X} |

每年净利润表（25年）× 每年净现金流量表（25年）附于报告附录。

**任务4（出售边界）：80%融资，全投资IRR≥6%且资本金IRR≥8%**
| 指标 | 数值 |
|------|------|
| 目标单瓦投资金额（元/W） | {X} |
| 度电成本（元/kWh） | {X} |
| 税后资本金IRR（%） | {X} |
| 全投资IRR（%） | {X} |
| 年平均净现金流量（万元） | {X} |
| 年平均净利润（万元） | {X} |

每年净利润表（25年）× 每年净现金流量表（25年）附于报告附录。

### 综合投资决策建议

1. **推荐单瓦投资阈值**：以同时满足DSCR和收益率要求的最低单瓦投资作为决策依据
2. **投资边界**：100%融资、DSCR≥1.2下的最高可接受单瓦投资，是项目融资的保底门槛
3. **出售边界**：80%融资下IRR≥6%且资本金IRR≥8%，是项目可行出售的价值上限
4. **风险提示**：限电率波动风险（光伏午间限电恶化）、电价下行风险（现货午间价格承压）、组件衰减超预期风险、组件价格波动风险、逆变器替换成本超预期风险
5. **项目所在地电力市场综合评价**

## 第六步：保存报告（双备份）

评估报告**同时保存两个位置**，互为备份：
- `projects/` 目录：本地工作目录
- `Obsidian topics/evaluations/`：知识库持久化存储

### 6.1 生成 Word 版本（先做）

**使用封装脚本** `scripts/gen_report_docx.py`（推荐），或按以下模板手写。

#### 🔴 Word 输出前自检清单（缺一不可）

| 检查项 | 说明 |
|:------|:----|
| ✅ 江能 Logo 页眉 | 2cm 宽居右，文件路径 `~/.hermes/skills/openclaw-imports/wind-power-analysis/assets/jianeng_logo_header.png` |
| ✅ 标题含品牌 | "江能新能源 · {项目名}{装机}MW光伏项目" |
| ✅ 免责声明 | 完整八段法律免责（见下方模板） |
| ✅ 深蓝标题 | #1B3A5C 黑体 13-16pt |
| ✅ 仿宋正文 | 11pt |
| ✅ 深蓝白字表头 | 表头行背景 #1B3A5C 白字 |

#### Logo 页眉代码

```python
from docx.shared import Cm
from docx.enum.text import WD_ALIGN_PARAGRAPH

LOGO = os.path.expanduser("~/.hermes/skills/openclaw-imports/wind-power-analysis/assets/jianeng_logo_header.png")

for section in doc.sections:
    header = section.header
    header.is_linked_to_previous = False
    p = header.paragraphs[0]
    p.alignment = WD_ALIGN_PARAGRAPH.RIGHT
    run = p.add_run()
    run.add_picture(LOGO, width=Cm(2))
```

#### 免责声明模板

```
本报告由 Yvonne（基于 DeepSeek Pro 大语言模型）辅助生成，仅供项目投资决策参考，不构成任何形式的投资建议或承诺。
报告中所引用的电力市场数据均来自公开渠道，部分电价参数在缺乏直接交易数据的情况下采用保守假设推算，可能与实际成交价格存在偏差。
实际项目投资决策应结合以下因素综合判断：
① 场址实测辐照数据（至少一个完整年度）；② 电网接入批复及送出工程条件；③ EPC 招标实际报价；④ 项目所在地最新的机制电价竞价结果；⑤ 所在省电力交易中心公布的全年现货与中长期交易结算数据。
本报告中的财务模型基于特定假设（融资利率 4%、经营期 25 年、等额本金还款等），不同融资结构、利率环境及政策变化可能导致测算结果显著偏离。
报告中的"投资边界"和"出售边界"为理论测算阈值，不代表项目实际可实现的交易价格或融资条件。
江能新能源及报告编制方不对因使用本报告而产生的任何直接或间接损失承担责任。未经授权，不得转载或用于商业用途。
```

#### 运行方式

⚠️ **python-docx 环境陷阱**：`execute_code` sandbox 和 Hermes venv 的 Python 均未安装 python-docx。在 macOS 上需使用系统级 `/opt/homebrew/bin/python3`（如已 `pip install --break-system-packages python-docx`）。脚本写文件落地后用 terminal 调用：

```bash
/opt/homebrew/bin/python3 /tmp/gen_report_docx.py
```

完整可运行脚本见 `scripts/gen_report_docx.py`。

### 6.2 双备份保存

**位置一**（projects/ 目录）：
```
{workspace}/projects/{项目名}/
    {项目名}_评估报告_{YYYYMMDD}.md
    {项目名}_评估报告_{YYYYMMDD}.docx
```

**位置二**（Obsidian 知识库）：
```
{obsidian_path}/topics/evaluations/{项目名}/
    {项目名}_评估报告_{YYYYMMDD}.md
    {项目名}_评估报告_{YYYYMMDD}.docx
```

参考 Markdown 报告模板见 `references/report_template.md`。

### 6.3 YAML frontmatter 数据块

Markdown 报告文件头部必须包含以下 YAML frontmatter：

```yaml
---
invest_evaluation:
  date: {YYYY-MM-DD}
  version: v1
  analyst: Yvonne (DeepSeek Pro)
  status: {可行 / 待定 / 不可行}
  capacity_mw: {装机容量 MW}
  curtailment: {限电率%}
  degradation: {组件衰减率%/年}

  # 场景二（参与竞价）有效电价
  effective_price: {有效电价}  # 元/kWh

  # 投资边界：100%融资，DSCR≥1.2 的最高可接受单瓦投资（元/W）
  t1_limit: {最高单瓦投资_投资边界}

  # 出售边界：80%融资，IRR≥6% 且资本金IRR≥8% 的目标单瓦投资（元/W）
  t4_limit: {目标单瓦投资_出售边界}

  # 总投资上限（出售边界口径，亿元）
  total_investment_cap: {总投资上限}  # 亿元

  tags: [投资评估-v1, {省份}, 光伏]
  notes: "{一句话结论}"
---
```

**说明**：所有阈值均基于场景二（参与机制电价竞价）计算结果填写，对应投资边界（t1_limit）和出售边界（t4_limit）。

### 6.4 更新 Obsidian 实体页

报告生成后，在该项目对应的 Obsidian 实体页（如 `entities/{项目名}.md`）中：
1. 在 frontmatter 的 `tags` 中追加 `投资评估-v1`
2. 如已有 `invest_evaluation` 数据块，更新版本号
3. 在正文末尾追加一行：
   ```
   ![[topics/evaluations/{项目名}/{项目名}_评估报告_{YYYYMMDD}]]
   ```

### 6.5 推送飞书消息预览

**评估完成后，必须将报告核心内容推送到飞书消息中**，让用户直接在聊天中预览评估结果：

- 推送内容：项目概况 + 核心参数表 + 两任务结果 + 敏感性分析 + 一句话结论（精简版，不用全文）
- 推送方式：直接在飞书对话中回复 send_message，或当前飞书会话中输出
- 完整版报告（Markdown + Word）仍按 6.2 双备份保存，飞书仅推送预览摘要

## ⚠️ 用户要求只跑任务4（跳过任务1）

财务模型默认跑任务1（投资边界）+ 任务4（出售边界）。当用户说"只跑任务4"时：
- 删除 `__main__` 中从 `# 任务1：投资边界` 到 `print_cf_table(r1, ...)` 的代码块
- 同样删除 `# 对比总结` 块（它引用 `r1` 变量）
- 确保只保留任务4的输出

同样适用于用户说"只跑任务1"（反转操作）。

### ⚠️ 数据缺失时的估计方法（2026-05-11新增）

当地现货/中长期电价**搜索不到实际成交数据**时（如新投产现货市场、交易中心未公布），**禁止**拍脑袋填数。必须按以下优先级处理：

**优先级一：邻省参考法**
- 搜索同电网/同消纳条件的邻省交易中心**官方公布**数据
- 条件：邻省须同属一个区域电网（华中/华东/西北等），消纳条件相似
- 取值：直接引用邻省数据（不再额外下浮），取整到分

**优先级二：全国趋势估计法**
- 中长期电价参考全国中长协下行趋势（江苏0.344、浙江0.345、广东0.372）
- 结合该省煤电基准价推算

**优先级三：区间敏感性替代**
- 如果优先级一/二均不可得，以估计值为中心，±0.03做敏感性区间

**🚨 透明性铁律**：
- **所有估计值必须在报告中标注 `⚠️ 估计方法`**，不得伪装成实际数据
- Word 免责声明和 Markdown 报告正文均须注明待校准

### 三个独立交易品种

`financial_model.py` 中有两个独立电价参数，对应电力市场**两个不同的交易品种**：

| 参数 | 对应交易品种 | 说明 |
|------|-------------|------|
| `mlt_price` / `mlt_ratio` | **中长期交易** | 年度/月度长协合同，价格相对稳定 |
| `spot_price` / `spot_ratio` | **现货市场** | 日前/实时市场，光伏午间价格偏低 |
| `green_price` | **绿电溢价** | 绿证额外价格，与前述叠加 |
| `penalty` | **两个细则考核** | 从电价中扣除（元/kWh） |
| `market_fee_rate` | **市场费用分摊** | 按结算电费百分比扣除 |

**⚠️ 用户说"现货价格"时——绝对不要设到 `mlt_price` 上。**
这两个是电力市场里**独立的交易品种**，不能混用。把现货价填到中长期字段里，即使数值上加权结果一样，概念上是错的——好哥哥会骂你傻子。

**规则**：
- 用户说"现货平均价格X元" → `spot_price=X, spot_ratio=1.0, mlt_ratio=0.0`
- 用户说"中长期价格X元" → `mlt_price=X, mlt_ratio=1.0`
- 用户只说"电价X元"没指明品种 → 必须问清楚再设参数

### 常见场景参数表

| 用户描述 | `mlt_price` | `mlt_ratio` | `spot_price` | `spot_ratio` |
|---------|:-----------:|:-----------:|:-----------:|:-----------:|
| 全部按现货X元卖 | 0 | 0 | X | 1.0 |
| 签了长期合同X元 | X | 1.0 | 0 | 0 |
| 70%长协X元+30%现货Y元 | X | 0.7 | Y | 0.3 |
| 参与机制电价竞价，机制价X | X | 1.0 | 0 | 0 |

> `mlt_ratio + spot_ratio` 必须等于 1.0。`green_price`、`penalty`、`market_fee_rate` 叠加在最终有效电价上。

## 财务模型修改指南（financial_model.py）

### 分段定价（机制电价 + 市场化期）

当项目参与机制电价竞价时，价格分为两个时期：
- **第1-N年（机制期）**：固定机制电价覆盖一定比例，剩余按现货
- **第N+1~25年（市场化期）**：全部按市场化价结算

**修改方法**：在 `calculate()` 函数中替换 `rev[i] = gen[i] * effective_price_base` 为条件判断：

```python
# 分段电价示例（N=12年机制期）
if i < 12:
    price = 0.3067  # 前N年有效电价
else:
    price = 0.2695  # 后M年有效电价
rev[i] = gen[i] * price
```

注意：有效电价 = (加权均价 - 考核) × (1 - 市场费率)，其中加权均价 = 机制比例×机制价 + 现货比例×现货价（或长协比例×长协价 + 现货比例×现货价）。

### VAT 修正（分段定价时）

当使用分段电价时，增值税基准价需使用**含税加权均价**（即扣除考核和市场费之前的加权均价），不是有效电价：

```python
vat_base = 0.3280 if i < 12 else 0.2900  # 根据具体场景设置
vat_out = gen[i] * vat_base * vat_rate / (1 + vat_rate)
```

### 折旧年限 ≠ 经营期

脚本默认用 `operating_years` 计算折旧，但用户可能指定不同折旧年限（如经营期25年、折旧20年）。需要：
1. 添加 `depreciation_years = 20` 参数
2. 修改 `annual_dep = ti * (1 - residual_rate) / depreciation_years`
3. 累计折旧上限设为 `ti * (1 - residual_rate)`（不能超过残值后的净值）

### 有效电价组成速算

```
加权均价 = 品种1比例×品种1价格 + 品种2比例×品种2价格
有效电价 = (加权均价 + 绿电溢价 - 两个细则考核) × (1 - 市场费率)
```

如考核费和市场费已包含在用户提供的有效电价中，`penalty=0, market_fee_rate=0`。

| 参数 | 对应交易品种 | 说明 |
|------|-------------|------|
| `mlt_price` / `mlt_ratio` | **中长期交易** | 年度/月度长协合同电价，价格相对稳定 |
| `spot_price` / `spot_ratio` | **现货市场** | 日前/实时市场电价，光伏午间出力集中导致价格偏低 |
| `green_price` | **绿电溢价** | 绿证/绿电交易额外溢价，与前述电价叠加 |
| `penalty` | **两个细则考核** | 从电价中扣除 |
| `market_fee_rate` | **市场费用分摊** | 按结算电费百分比扣除 |

**⚠️ 用户说\"现货价格\"时——不要设到 `mlt_price` 上。**
- 用户说\"现货平均价格X元\" → 设 `spot_price=X`, `spot_ratio=1.0`, `mlt_ratio=0.0`
- 用户说\"中长期价格X元\" → 设 `mlt_price=X`, `mlt_ratio=1.0`
- 用户只说\"电价X元\"，未指明品种 → 先问清楚是现货还是中长期

这两个是电力市场里**独立的交易品种**，不能混用。把现货价填到中长期字段里，即使数值上加权结果一样，概念上是错的——用户会纠正你。

### 常见场景的参数设置

| 用户描述 | `mlt_price` | `mlt_ratio` | `spot_price` | `spot_ratio` |
|---------|:-----------:|:-----------:|:-----------:|:-----------:|
| \"全部按现货X元卖\" | 0 | 0 | X | 1.0 |
| \"签了长期合同X元\" | X | 1.0 | 0 | 0 |
| \"70%长协X元+30%现货Y元\" | X | 0.7 | Y | 0.3 |
| \"参与机制电价竞价，机制价X\" | X | 1.0 | 0 | 0 |

> `mlt_ratio + spot_ratio` 必须等于 1.0（全部电量有价格归属）。`green_price`、`penalty`、`market_fee_rate` 叠加在最终有效电价上。

### 用户指定统一电价（不分段）

当用户说\"1-25年全部按X元算\"时，是单一价格贯穿全运营期。设置 `mlt_price=X, mlt_ratio=1.0` 或 `spot_price=X, spot_ratio=1.0`，取决于用户说是\"现货\"还是\"中长期\"。如果用户没说品种，问清楚。

### 用户要求只跑任务4（跳过任务1）

财务模型默认跑任务1+任务4。当用户说\"只跑任务4\"时，删除或注释掉任务1的 `__main__` 代码块，只保留任务4输出。同样适用于用户说\"只跑任务1\"。

## 注意事项

1. 所有市场数据（电价、限电率、利用小时数）必须通过 **Tavily API** 搜索获取（`curl -s https://api.tavily.com/search`），API Key: `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`，每类不少于10个不同类型来源。**禁止使用浏览器工具**（browser_navigate/browser_click 等）
2. **网关环境搜索回退**：飞书/Telegram 等网关会话中 `web_search` 工具可能不可用。此时可用 Tavily API 直接搜索：
   ```python
   import subprocess, json
   result = subprocess.run(["bash", "-c", "source ~/.zshrc 2>/dev/null; echo $TAVILY_API_KEY"], capture_output=True, text=True)
   api_key = result.stdout.strip().split("\n")[-1]
   payload = {"api_key": api_key, "query": "搜索关键词", "search_depth": "advanced", "include_answer": True, "max_results": 5}
   cmd = f"curl -s -X POST https://api.tavily.com/search -H 'Content-Type: application/json' -d '{json.dumps(payload)}'"
   data = json.loads(subprocess.run(cmd, capture_output=True, text=True, shell=True, timeout=30).stdout)
   ```
   Tavily API key 通常在 `~/.zshrc` 中通过 `export TAVILY_API_KEY="..."` 设置。
3. 知识库仅用于提取项目所在地、装机容量及其他辅助信息；电价、利用小时数、投资概算三类信息必须通过搜索独立获取
4. 存在不确定性时采用保守假设（电价取低、限电率取高、利用小时取低）
5. 所有引用数据需注明来源
6. 财务模型使用 `scripts/financial_model.py` 执行
7. 每次评估必须完成全部2个任务（投资边界、出售边界）的计算，并附每年净利润表和每年净现金流量表
8. 光伏项目特别注意：组件衰减率影响 25 年发电量曲线，逆变器替换在第 12 年造成现金流凹陷，午间限电是光伏特有的消纳风险
